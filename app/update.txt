═══════════════════════════════════════════════════════════════
  ACM PROJECT — BACKEND CHAIN UPDATE-1-(shreya)
  Autonomous Collision Management System
═══════════════════════════════════════════════════════════════
 
WHAT WE BUILT
─────────────────────────────────────────────────────────────
Full backend pipeline wired into FastAPI @ localhost:8000
covering all 4 nodes of the backend chain:
 
  Ground Stations CSV
        ↓
  LOS Checker  →  Blackout Zones
        ↓
  RTN Frame Calculator  →  Maneuver Planner
        ↓
  Station-Keeping Box  →  Recovery Burn Trigger
 
 
CHAIN LINK 1 — GROUND STATION NETWORK (§5.5.1)
─────────────────────────────────────────────────────────────
File: app/api/ground_stations.py
 
8 real-world ground stations loaded from ground_stations.csv:
  Svalbard · Fairbanks · Maspalomas · Canberra
  Kourou · Bangalore · Santiago · Hartebeesthoek
 
What it does:
  • Reads lat/lon/alt/elevation-mask from CSV at runtime
  • Computes elevation angle for any satellite ECI state
  • Detects LOS acquisition and loss events across the pass
  • Outputs pass windows (start, end, max elevation, duration)
  • Outputs blackout windows (gaps with zero station coverage)
  • Reports coverage % over the full track
 
Key endpoint:
  POST /api/ground-stations/los-check
  GET  /api/ground-stations/list
 
Latency & blackout constants (§5.4):
  LEO round-trip  ~5 ms   |  Max blackout  ~12 min
  MEO round-trip  ~50 ms  |  Horizon mask  5°
  GEO round-trip  ~600 ms |  Relay delay   +2 ms/hop
 
 
CHAIN LINK 2 — RTN FRAME + MANEUVER PLANNER (§5.2)
─────────────────────────────────────────────────────────────
File: app/api/maneuver.py
 
Converts raw ECI position/velocity into the Radial-Tangential-
Normal local orbital frame, then plans corrective burns.
 
RTN axes:
  R  →  radial outward from Earth centre
  T  →  tangential, direction of motion
  N  →  normal, perpendicular to orbital plane
 
3 maneuver types supported:
  hohmann     — altitude raise/lower (purely tangential ΔV)
  inclination — cross-track correction (normal ΔV at node)
  phasing     — along-track phase error correction
 
Every maneuver returns:
  • ΔV vector in RTN frame  (R, T, N components)
  • ΔV vector in ECI frame  (x, y, z components)
  • ΔV magnitude in m/s
  • Estimated burn duration in seconds
  • Orbital mechanics notes
 
Validation enforced:
  • Unknown satellite ID     → REJECTED
  • ΔV > 15 m/s per burn     → REJECTED
  • Thruster cooldown < 600s → REJECTED
  • Insufficient fuel        → REJECTED
  • No ground station LOS    → REJECTED
 
Key endpoints:
  POST /api/maneuver/schedule    — validate & queue burn sequence
  POST /api/maneuver/rtn-frame   — compute RTN axes for ECI state
  POST /api/maneuver/plan        — plan a single maneuver
  GET  /api/maneuvers/active     — all pending burns in constellation
 
 
CHAIN LINK 3 — STATION-KEEPING BOX (§5.3)
─────────────────────────────────────────────────────────────
File: app/api/station_keeping.py
 
Maintains a dead-band box in RTN coordinates around each
satellite's nominal orbital slot. Flags drift before it
becomes a collision risk.
 
Box limits by shell:
  LEO  →  ±1 km radial  |  ±5 km tangential  |  ±2 km normal
  MEO  →  ±2 km radial  |  ±10 km tangential |  ±3 km normal
  GEO  →  ±5 km radial  |  ±37 km tangential |  ±5 km normal
 
3-tier status system:
  OK        — satellite within box
  WARNING   — satellite at 80%+ of limit (pre-alert)
  VIOLATED  — satellite outside box → burn triggered
 
Key endpoint:
  POST /api/station-keeping/check
 
 
CHAIN LINK 4 — RECOVERY BURN TRIGGER
─────────────────────────────────────────────────────────────
File: app/api/station_keeping.py  (same file, auto-fires on violation)
 
When a VIOLATED status is detected:
  1. Identifies which axes are violated (R / T / N)
  2. Calculates corrective ΔV using CW (Clohessy-Wiltshire)
     linearised equations
  3. Determines burn type:
       single-axis  →  radial / tangential / normal correction
       multi-axis   →  combined burn (priority 1, fires soonest)
  4. Schedules burn 5 minutes from detection (uplink window)
  5. Queues in priority-sorted burn queue
 
Priority levels:
  1 — combined violation (immediate)
  2 — radial or normal violation
  3 — tangential-only (least urgent, along-track drift)
 
Key endpoints:
  GET    /api/station-keeping/burn-queue        — view pending burns
  DELETE /api/station-keeping/burn-queue/{id}   — clear burns for sat
 
 
STRESS TEST RESULTS
─────────────────────────────────────────────────────────────
  ✅  50 satellites propagated per step
  ✅  500 debris objects tracked
  ✅  10 simulate/step calls completed
  ✅  9 collision events detected & maneuvers executed
  ✅  Fuel depletion check passed (0 sats below 45 kg)
  ✅  Valid maneuver accepted
  ✅  Over-limit burn rejected  (>15 m/s)
  ✅  Unknown satellite rejected
  ✅  Active maneuvers endpoint live
  ⚠   Snapshot performance: ~2050ms (caching applied, monitor)
 
 
BUGS FIXED DURING SESSION
─────────────────────────────────────────────────────────────
  1. Duplicate router = APIRouter() in maneuver.py
     → Silently wiped /schedule, /active, /rtn-frame endpoints
     → Fixed: single router declaration at top of file
 
  2. /api/maneuvers/active returning 404
     → Endpoint was indented inside plan_maneuver() function
       body — Python never registered it as a route
     → Fixed: moved to top-level @router.get
 
  3. Over-limit & unknown-satellite burns not rejected
     → Validation errors were collected but rejection block
       had a logic gap — valid burns could still be SCHEDULED
     → Fixed: hard checks at top of schedule_maneuver()
 
  4. Snapshot endpoint returning ~2100ms
     → Full 550-object state rebuilt on every poll
     → Fixed: 500ms TTL cache, invalidated after each /step
 
 
FILES DELIVERED
─────────────────────────────────────────────────────────────
  app/api/ground_stations.py   — chain links 1
  app/api/maneuver.py          — chain links 2  (+ bug fixes)
  app/api/station_keeping.py   — chain links 3 & 4
  app/api/simulate.py          — snapshot cache + /active fix
  ground_stations.csv          — 8 stations, project root
  main_additions.py            — router registration snippet

  ═══════════════════════════════════════════════════════════════
 ----------------------------------------------------------------------------------------------------------------
  ACM PROJECT — CRITICAL LOS FIX---UPDATE-2(shreya)
  Autonomous Collision Management System
  Update #2
 
═══════════════════════════════════════════════════════════════
 
ISSUE IDENTIFIED
─────────────────────────────────────────────────────────────
Ground Station LOS was completely absent from the system.
 
The problem statement defines LOS as a HARD CONSTRAINT —
maneuvers cannot be uploaded unless the satellite has line-of-
sight to at least one of the 6 ground stations. The grader
tests this explicitly by sending a maneuver for a satellite
over the South Pacific where no station has visibility.
 
The old code in check_ground_station_los() was:
 
    def check_ground_station_los(sat) -> bool:
        r_norm = float(np.linalg.norm(sat.r))
        alt_km = r_norm - 6378.137
        return 200.0 <= alt_km <= 2000.0   ← ALWAYS TRUE
 
This returned True for every LEO satellite regardless of its
position over Earth. The grader would:
  • Send maneuver for sat over South Pacific
  • System accepts it  (wrong)
  • Grader rejects it as invalid
  • Zero credit for blind conjunction pre-upload scenario
  • Safety Score (25% of total) takes direct hit
 
 
WHAT WE BUILT — app/api/los.py
─────────────────────────────────────────────────────────────
New self-contained module using real orbital geometry.
Zero new pip installs — stdlib only (csv, math, time, pathlib)
plus FastAPI which is already in requirements.txt.
 
How it works (step by step):
  1. Loads ground_stations.csv once at startup via lru_cache
     Falls back to 6 hard-coded stations if CSV not found
  2. Converts each station geodetic lat/lon/alt → ECEF (km)
     using the WGS-84 ellipsoid for accuracy
  3. Rotates station ECEF → ECI via Greenwich Apparent Sidereal
     Time (GAST) — accounts for Earth's rotation at the exact
     simulation epoch, not wall-clock time
  4. Computes elevation angle from each station to satellite:
       elev = arcsin( dot(range_vec, up_vec) / |range_vec| )
  5. Returns True if ANY station sees satellite above its
     minimum elevation mask (default 5 degrees)
 
A satellite over the South Pacific now correctly returns False
because none of the 6 stations have a 5deg+ elevation angle to
that position at any given epoch.
 
 
KEY FUNCTIONS
─────────────────────────────────────────────────────────────
  has_ground_station_los(sat_r_km, epoch_unix, csv_path)
    → bool
    Called from schedule_maneuver() in maneuver.py
    The single source of truth for LOS decisions
 
  los_details(sat_r_km, epoch_unix, csv_path)
    → dict with per-station elevation angles + visibility
    Added to both SCHEDULED and REJECTED responses so the
    grader can inspect exactly which stations were checked
 
Bonus REST endpoint (register in main.py):
  GET /api/los/check?x=6871&y=0&z=0
  → Instant browser/curl test for any ECI position
 
 
CHANGES TO maneuver.py
─────────────────────────────────────────────────────────────
Only 3 lines changed in the existing file:
 
  1. Added import:
       from app.api.los import has_ground_station_los, los_details
 
  2. Replaced fake function with real geometry call:
       def check_ground_station_los(sat, epoch_unix=None):
           return has_ground_station_los(sat.r, epoch_unix)
 
  3. Passed sim epoch into the check:
       los_ok = check_ground_station_los(sat, epoch_unix=current_time)
 
  4. Added los_details to both REJECTED and SCHEDULED responses
 
 
SCORING IMPACT
─────────────────────────────────────────────────────────────
  Before fix:
    • LOS always True → grader's South Pacific test accepted
    • Blind conjunction scenario: 0 credit
    • Safety Score: penalised
 
  After fix:
    • LOS computed from real elevation angle geometry
    • South Pacific satellite correctly REJECTED
    • Blind conjunction pre-upload scenario: full credit path
    • Safety Score (25%): no longer taking a direct hit
    • Per-station breakdown in response aids grader inspection
 
 
FILES DELIVERED IN THIS UPDATE
─────────────────────────────────────────────────────────────
  app/api/los.py       — NEW: full LOS geometry engine
  app/api/maneuver.py  — UPDATED: real LOS wired in
 
═══════════════════════════════════════════════════════════════
----------------------------------------------------------------------------------------------
ACM PROJECT — SESSION UPDATE LOG-3(avneesh)
Autonomous Collision Management System
═══════════════════════════════════════════════════════════════


SESSION SUMMARY
─────────────────────────────────────────────────────────────
Fixed 4 critical issues from the review document.
All changes are isolated to 3 files:

  app/main.py              — Issues 3, 9, 10 + broken router merge
  app/maneuver/planner.py  — Issues 4 + 5
  Dockerfile               — Issue 9
  requirements.txt         — Issue 10


═══════════════════════════════════════════════════════════════
ISSUE 3 — ECI → lat/lon was using wall-clock time (FIXED)
═══════════════════════════════════════════════════════════════

File: app/main.py → snapshot() → eci_to_latlon()

Problem:
  The snapshot endpoint converted ECI positions to lat/lon using
  datetime.now() to compute Earth's rotation. This means after
  fast-forwarding the simulation 6 hours via /simulate/step, all
  satellites appeared at the correct ECI coordinates but wrong
  Earth-relative longitudes — everything was shifted west.

Fix:
  Now uses Greenwich Sidereal Time (GST) computed from simulation
  elapsed time (get_sim_time()), not wall-clock time.

  GST formula used:
    GST0     = 280.46061837 degrees  (J2000 epoch)
    EARTH_ROT = 360.98564724 / 86164.1  degrees per second
    gst_deg  = (GST0 + EARTH_ROT × sim_time_s) % 360

  ECI longitude is then rotated by subtracting gst_deg before
  returning the ECEF longitude to the frontend.

Impact:
  Globe now shows satellites at correct Earth-relative positions
  even after large simulation time jumps. UI score (15%) protected.


═══════════════════════════════════════════════════════════════
ISSUE 4 — Evasion direction wrong for many approach geometries (FIXED)
═══════════════════════════════════════════════════════════════

File: app/maneuver/planner.py → compute_evasion_dv()

Problem:
  Old code always burned in the transverse (prograde/retrograde)
  direction regardless of how the debris was actually approaching.
  For head-on approaches, a prograde burn increases closing speed.
  For debris approaching from above/below, transverse burn does nothing.

Fix:
  New function compute_evasion_dv() uses actual approach geometry:

  1. Computes RTN axes (Radial, Transverse, Normal) from sat state
  2. Finds relative position vector at Time of Closest Approach (TCA)
     using linear approximation
  3. Decomposes relative position into RTN components
  4. Chooses burn axis based on dominant approach direction:

     Debris approaching transversely (head-on / tail-on)
       → Burn RADIAL — shifts altitude, changes crossing time

     Debris approaching radially (from above / below)
       → Burn TRANSVERSE — most fuel-efficient phase shift

     Debris approaching normally (out of plane)
       → Burn TRANSVERSE — avoids expensive plane-change burns

  5. Burn sign chosen to push satellite AWAY from debris at TCA

New helpers added:
  compute_rtn_axes(r, v)         — returns R, T, N unit vectors in ECI
  rtn_to_eci(dv_rtn, r, t, n)   — converts RTN delta-v to ECI frame

Impact:
  Evasion now geometrically correct for all approach angles.
  Safety Score (25%) and Fuel Efficiency (20%) both protected.


═══════════════════════════════════════════════════════════════
ISSUE 5 — Recovery burn hardcoded at T+660s (FIXED)
═══════════════════════════════════════════════════════════════

File: app/maneuver/planner.py → execute_scheduled_burns()
                               → _schedule_recovery_when_clear()
                               → _threat_still_active()

Problem:
  Recovery burn was always scheduled at current_time + 660s
  (600s cooldown + 60s buffer), regardless of when the debris
  actually clears. If TCA is 2 hours away, recovery fires while
  satellite is still in the conjunction zone.

Fix:
  Recovery burns now store the threat debris ID and check if the
  threat has actually cleared before firing.

  New function _threat_still_active(sat_id, debris_id):
    → Computes current distance between satellite and debris
    → Returns True if distance < 0.5 km (SAFE_DISTANCE_KM)

  In execute_scheduled_burns():
    → If recovery burn is due AND threat still active
      → Delay burn by RECOVERY_CHECK_INTERVAL_S (300s = 5 min)
      → Retry next simulation tick
    → If threat has cleared AND cooldown expired
      → Fire recovery burn, set sat.status = "NOMINAL"

  Scheduled burn dict now stores:
    "threat_debris_id"  — which debris to check against
    "min_clear_time"    — earliest possible recovery time

Impact:
  Recovery burns no longer fire into active conjunction zones.
  Constellation Uptime (15%) and Safety Score (25%) protected.


═══════════════════════════════════════════════════════════════
ISSUE 9 — Docker only served backend (FIXED)
═══════════════════════════════════════════════════════════════

Files: Dockerfile (full rewrite)
       app/main.py (static file serving added)
       requirements.txt (aiofiles added)

Problem:
  Dockerfile only built and served the Python backend on port 8000.
  The React frontend only worked in local dev via Vite on port 3000.
  For the video demo and any judge testing, both needed to work
  from a single Docker container.

Fix — Dockerfile is now multi-stage:

  Stage 1 (node:20-slim):
    → Copies frontend/ source
    → Runs npm install + npm run build
    → Outputs production bundle to /frontend/dist

  Stage 2 (ubuntu:22.04 — required base image):
    → Copies Python backend
    → Copies built frontend from Stage 1 into /acm/static/
    → Runs uvicorn on port 8000

Fix — app/main.py now serves frontend:

  If /acm/static/ directory exists (i.e. running in Docker):
    → Mounts /assets via FastAPI StaticFiles
    → Catch-all route serves index.html for all non-API paths
    → React SPA routing works correctly

  If /acm/static/ does not exist (local dev):
    → Static serving is skipped entirely
    → Vite on port 3000 works exactly as before
    → Zero changes to local dev workflow

Impact:
  Single docker run -p 8000:8000 acm serves full stack.
  Video demo works from Docker. Grader auto-test unaffected.


═══════════════════════════════════════════════════════════════
ISSUE 10 — requirements.txt incomplete (FIXED)
═══════════════════════════════════════════════════════════════

File: requirements.txt

Added:
  aiofiles==23.2.1       — required by FastAPI StaticFiles
  python-dateutil==2.9.0 — required for ISO timestamp parsing

Full requirements.txt now:
  fastapi==0.111.0
  uvicorn==0.29.0
  pydantic==2.7.1
  numpy==1.26.4
  scikit-learn==1.4.2
  aiofiles==23.2.1
  python-dateutil==2.9.0


═══════════════════════════════════════════════════════════════
BROKEN main.py MERGE (FIXED)
═══════════════════════════════════════════════════════════════

File: app/main.py

Problem:
  The teammate's ground station / station-keeping router additions
  were indented as if inside a function body and created a second
  app = FastAPI(...) instance at the bottom of the file. This:
    → Overrode the first app instance
    → Wiped out all telemetry / simulate routes
    → CORS middleware was never applied
    → gs_router and sk_router were never registered

Fix:
  Complete clean rewrite of main.py with single app instance,
  all 5 routers registered at top level:
    telemetry_router   → /api/telemetry
    simulate_router    → /api/simulate
    maneuver_router    → /api/maneuver
    gs_router          → /api/ground-stations
    sk_router          → /api/station-keeping

  CORS middleware properly added before all routes.
  All imports at module level (not inside functions).


═══════════════════════════════════════════════════════════════
FILES CHANGED THIS SESSION
═══════════════════════════════════════════════════════════════

  app/main.py              REPLACED  — GST fix, router merge fix,
                                       static serving, CORS
  app/maneuver/planner.py  REPLACED  — geometry-aware evasion,
                                       TCA-aware recovery
  Dockerfile               REPLACED  — multi-stage Node + Python build
  requirements.txt         REPLACED  — added aiofiles, python-dateutil


═══════════════════════════════════════════════════════════════
FILES NOT TOUCHED — SAFE FOR TEAMMATES
═══════════════════════════════════════════════════════════════

  app/api/telemetry.py       — unchanged
  app/api/simulate.py        — unchanged
  app/api/maneuver.py        — unchanged
  app/api/ground_stations.py — unchanged (teammate owns)
  app/api/station_keeping.py — unchanged (teammate owns)
  app/orbit/propagator.py    — unchanged
  app/collision/             — unchanged
  app/prediction/tca.py      — unchanged (teammate warning: do not
                               change return signature of propagated_tca)
  app/models/                — unchanged
  app/config.py              — unchanged
  frontend/                  — unchanged
  stress_test.py             — unchanged


═══════════════════════════════════════════════════════════════
TEAMMATE WARNINGS
═══════════════════════════════════════════════════════════════

  1. prediction/tca.py
     planner.py imports propagated_tca(sat_r, sat_v, deb_r, deb_v).
     Do NOT change the function signature or return format.
     Returns: (t_ca_seconds: float, min_distance_km: float)

  2. maneuver/planner.py
     This file is now fully owned by this branch.
     Do not edit plan_maneuver() or execute_scheduled_burns()
     without syncing first.

  3. app/main.py
     Router registration is now clean and complete.
     If adding new routers, append to the registration block
     at the top — do not create a second app instance.


═══════════════════════════════════════════════════════════════
ACM PROJECT — BACKEND CHAIN UPDATE-4-(shreyansh)
Autonomous Collision Management System
═══════════════════════════════════════════════════════════════


1. STRUCTURED EVENT LOGGING (Problem 6) — app/logger.py [NEW FILE]
   - Added complete structured logging system
   - Every CDM detection, maneuver planned/executed, recovery scheduled/executed,
     EOL trigger, and graveyard burn now writes a JSON line to acm_events.log
   - In-memory ring buffer (last 500 events) exposed via GET /api/events
   - Zero new dependencies — uses Python standard library only

2. NOMINAL SLOT PROPAGATION (Problem 7) — app/models/satellite.py [UPDATED]
   - propagate_nominal() now uses same RK4+J2 propagator as real position
   - Eliminates phantom station-keeping penalties caused by nominal slot
     drifting differently from real satellite position
   - Added distance_to_slot() and in_station_keeping_box() as proper methods
   - Fixed cooldown_remaining property (was always returning 0.0) →
     replaced with is_on_cooldown(sim_time) method

3. PLANNER LOGGING INTEGRATION — app/maneuver/planner.py [UPDATED]
   - All 6 logging call sites wired into Avneesh's geometry-aware planner
   - Graveyard burn direction fixed: was retrograde (sat.v -= dv, deorbit)
     corrected to prograde (sat.v += dv, raises apogee to graveyard orbit)
   - Every decision point now logged including rejections (cooldown, no fuel)
     so grader audit trail is complete

═══════════════════════════════════════════════════════════════
Issue #8 — Snapshot payload size choking the frontend
═══════════════════════════════════════════════════════════════
What was wrong

/api/visualization/snapshot serialized all debris unconditionally every 2s poll
No altitude or proximity filtering — 10k objects = ~350KB/poll even with tuples
Globe3D destroyed and recreated the entire InstancedMesh every tick (full GPU realloc)

═══════════════════════════════════════════════════════════════
What changed
app/main.py

Added band query param (default leo) — filters debris to 160–2000 km altitude only
Added proximity_km query param (default 500) — skips debris farther than 500 km ECI from every tracked satellite
Added _eci_distance_sq() helper using squared distance (no sqrt) for fast culling
Response now includes debris_total / debris_shown so filter efficiency is observable

frontend/src/App.jsx

Snapshot fetch now passes band and proximity_km params
HUD debris counter shows 247/10000 ratio when filtering is active
SNAP_BAND and SNAP_PROXIMITY_KM constants at top of file for easy tuning

frontend/src/components/Globe3D.jsx

Reuse InstancedMesh across polls instead of destroy/recreate every 2s
Only reallocates GPU buffer when live count exceeds current capacity
Uses mesh.count to hide unused tail when filtered set shrinks
═══════════════════════════════════════════════════════════════
Impact
Payload (10k debris)Before (tuples, no filter)~350 KB/pollAfter (LEO band + proximity)~10–40 KB/poll
Not changed

Tuple format [ID, lat, lon, alt] was already spec-compliant ✓
═══════════════════════════════════════════════════════════════
Follow-up

Add GZipMiddleware to FastAPI for a further ~70% compression with one line
since param stubbed in for future delta-mode support

